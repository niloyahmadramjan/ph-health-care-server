model appointment {
  id        String            @id @default(uuid())
  status    AppointmentStatus @default(PEDDING)
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt
  payment   payment?

  @@map("appointments")
}
